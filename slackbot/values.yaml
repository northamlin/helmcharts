# Default values for slackbot.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

deployname: slackbot
gitsha: latest
slacktoken: token

# Was hardcoded to `ebs-sc`, an EKS class that does not exist on eu-upcloud-1.
storageClass: standard
